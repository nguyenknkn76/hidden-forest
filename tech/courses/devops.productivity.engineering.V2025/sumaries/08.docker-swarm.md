#

- Create VPC ...
- Create EC2 instances cluster (master, worker) > EC2 config: all access for sec group 
- ssh to master, and worker EC2
- In master: 
  - install docker, and docker compose: update device > install docker >  
> note: user don't have permission to run docker 
> 1. using `chmod 777 /var/run/docker.sock`
> 2. 
> `sudo systemctl enable docker`
> `sudo systemctl start docker`
> `sudo usermod -aG docker ec2user` ---> add user to group `docker`

container orchestration:  worker of docker
automation pipeline:      agent of pipeline

How to init cluster docker swarm?
1. `docker swarm init` in master
> require port of docker swarm 
> ---> open port in EC2 
> ssh: 22
> nodes <---> nodes (internal only): 2377t, 7946t, 7946u, 4789u
> configs: security group of EC2 instances ---> open port base on `subnet` or `security group rules` (best practice)
> don't fixed `security group` ---> easy to scale 

2. `docker swarm join ...` in worker EC2 instances 
> `docker swarm leave` to out cluster (in worker)
> `docker node ls` list docker swarm node (in master)
3.  In ur device: src > Dockerfile > docker images ---push to registry---> Docker Hub
> Docker Hub > create repo (access modify: public or private)
4. In master: using stack-file.yml 
  - run `docker stack deploy ...` (note: incase repo is private > run `docker login` via username+access_token)
  - run `docker network create ...`
5. Access our application via EC2 public IP